#!/usr/local/bin/node
const FS=require('fs');const PATH=require('path');const WEBTAG=require('./webtag');const MINIFY=require('./minify');const EXTEND=require('./extend');var watchPath=process.argv[2];!watchPath&&(void 0,process.exit(1)),void 0,FS.watch(watchPath,{recursive:true},(a,b)=>{b.endsWith('.tag.html')&&WEBTAG.create(watchPath+b),b.endsWith('.js')&&!PATH.dirname(b).endsWith('/dist')&&MINIFY.minify(watchPath+b),b.endsWith('.tag.js')&&EXTEND.extend(watchPath+b)});